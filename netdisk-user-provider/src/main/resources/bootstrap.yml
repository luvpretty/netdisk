server:
  port: 8010


spring:
  profiles:
    active:
      - dev
  application:
    name: netdisk-user-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml


